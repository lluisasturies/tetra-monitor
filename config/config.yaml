# Este fichero esta ignorado por Git y no se versiona.
# Copia config.yaml.example y renombralo a config.yaml para empezar:
#
#   cp config/config.yaml.example config/config.yaml
#
# A continuacion edita config.yaml con tus valores reales.
