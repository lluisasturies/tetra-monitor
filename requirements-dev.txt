# Dependencias de desarrollo — no se instalan en producción (RPi)
# Instalar con: pip install -r requirements-dev.txt

ruff==0.4.4
pytest==8.2.0
httpx==0.27.0
bcrypt==4.2.1
python-multipart==0.0.9
psycopg2-binary==2.9.10
